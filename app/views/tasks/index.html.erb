<div class="container container-user ">

<h1><%= Task.model_name.human%></h1><br/>

<!-- Check if link from project -->
<em><%= link_to @project.project_name, project_path(id: @project.id) if @project.present?%></em> 
<!-- Only allow factory and admin create cost -->
<div class="text-right"><%= link_to t('new_label'), new_task_path(project_id_param: @project.id)if (current_user.role == 'factory'||'admin') %></div>
<hr class="hr-table">
<!-- Cost list -->
<%= render "index_table"%>


</div>